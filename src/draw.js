  // A module for managing (WebGL) draw calls
  drawList.updateDrawCalls = function() {
    logDebug("drawList.updateDrawCalls");
    for (var i = 0; i < arguments.length; ++i) {
      var arg = arguments[i],
          path = this.paths[arg];
      if (typeof path == 'undefined') {
        for (var j = 0; j < scene; ++j)
      }
    }
  };

